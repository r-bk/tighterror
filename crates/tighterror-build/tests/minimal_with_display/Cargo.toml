[package]
name = "test_minimal_with_display"
edition = "2021"
version = "0.0.0"

[dependencies]
tighterror = { path = "../../../.." }

[build-dependencies]
env_logger = "0.10"
tighterror-build = { path = "../.." }
