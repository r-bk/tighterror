[package]
name = "test_minimal"
edition = "2021"
version = "0.0.0"

[dependencies]
tighterror = { path = "../../../.." }

[build-dependencies]
env_logger = "0.11.1"
tighterror-build = { path = "../.." }

[dev-dependencies]
anyhow = "1.0"

# force this package out of the main workspace
[workspace]
